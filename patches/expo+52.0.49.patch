diff --git a/node_modules/expo/android/build.gradle b/node_modules/expo/android/build.gradle
index 190ac19..49c4657 100644
--- a/node_modules/expo/android/build.gradle
+++ b/node_modules/expo/android/build.gradle
@@ -13,10 +13,20 @@ static def versionToNumber(major, minor, patch) {
   return patch * 100 + minor * 10000 + major * 1000000
 }
 
+// Android Studio's Gradle daemon doesn't inherit the shell PATH on Mac (Homebrew's
+// node isn't found via a bare "node" lookup, unlike a plain terminal/CLI Gradle run).
+static def resolveNodeBinaryForRNVersion() {
+  def candidates = ['/opt/homebrew/bin/node', '/usr/local/bin/node']
+  for (candidate in candidates) {
+    if (new File(candidate).exists()) return candidate
+  }
+  return 'node'
+}
+
 def getRNVersion() {
   def nodeModulesVersion = providers.exec {
     workingDir(projectDir)
-    commandLine("node", "-e", "console.log(require('react-native/package.json').version);")
+    commandLine(resolveNodeBinaryForRNVersion(), "-e", "console.log(require('react-native/package.json').version);")
   }.standardOutput.asText.get().trim()
 
   def version = safeExtGet("reactNativeVersion", nodeModulesVersion)
diff --git a/node_modules/expo/scripts/autolinking.gradle b/node_modules/expo/scripts/autolinking.gradle
index 929b7f0..4eafbdd 100644
--- a/node_modules/expo/scripts/autolinking.gradle
+++ b/node_modules/expo/scripts/autolinking.gradle
@@ -1,5 +1,15 @@
+// Android Studio's Gradle daemon doesn't inherit the shell PATH on Mac (Homebrew's
+// node isn't found via a bare "node" lookup, unlike a plain terminal/CLI Gradle run).
+def resolveNodeBinary = {
+  def candidates = ['/opt/homebrew/bin/node', '/usr/local/bin/node']
+  for (candidate in candidates) {
+    if (new File(candidate).exists()) return candidate
+  }
+  return 'node'
+}
+
 // Resolve `expo` > `expo-modules-autolinking` dependency chain
-def autolinkingPath = ["node", "--print", "require.resolve('expo-modules-autolinking/package.json', { paths: [require.resolve('expo/package.json')] })"]
+def autolinkingPath = [resolveNodeBinary(), "--print", "require.resolve('expo-modules-autolinking/package.json', { paths: [require.resolve('expo/package.json')] })"]
 apply from: new File(
   providers.exec {
     workingDir(rootDir)
